/usr/bin/ssh-add --apple-use-keychain ~/.ssh/id_rsa
/usr/bin/ssh-add --apple-use-keychain ~/.ssh/id_ecdsa

alias ll='ls -GalF'
alias la='ls -GA'
alias l='ls -GCF'

if [[ -x "`which mise`" ]]; then
  eval "$(mise activate zsh)";
fi
